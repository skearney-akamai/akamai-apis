description: |
  The absolute URL of the newly created custom PII type. Returned in the 201 Created response so you can retrieve the new resource.
required: true
schema:
  type: string
  
  example: https://{hostname}/appsec/v1/fai-configurations/12345/versions/3/custom-pii-types/internal-project-code
