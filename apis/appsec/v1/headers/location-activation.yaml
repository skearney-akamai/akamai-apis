description: |
  The absolute URL of the newly created activation. Returned in the 201 Created response so you can retrieve the new resource.
required: true
schema:
  type: string
  
  example: https://{hostname}/appsec/v1/fai-configurations/12345/activations/98766
