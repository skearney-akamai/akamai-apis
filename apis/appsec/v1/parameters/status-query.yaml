name: status
in: query
required: false
schema:
  type: string
  enum:
    - ACTIVE
    - INACTIVE
    - PENDING
    - FAILED
    - DEACTIVATED
description: Filter results by activation or configuration status.
example: "ACTIVE"
