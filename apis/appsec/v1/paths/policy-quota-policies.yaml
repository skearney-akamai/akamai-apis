x-akamai:
  # These are no-op, & clarify what endpoint this file's methods apply to
  path-info: /configs/{configId}/versions/{versionNumber}/security-policies/{policyId}/quota-policies
parameters:
- $ref: ../parameters/config-id-path.yaml
- $ref: ../parameters/version-number-path.yaml
- $ref: ../parameters/policy-id-path.yaml
get:
  operationId: get-policy-quota-policy-actions
  summary: List quota policy actions
  tags:
  - Quota policy actions
  description: |
    Review all quota policy actions currently assigned to a specific security policy.
  x-akamai:
    labels:
    - All products
  responses:
    200:
      description: |
        Successfully returned quota policy actions for this security policy.
      content:
        application/json:
          example:
            $ref: ../examples/quota-policy-actions-200.json
          schema:
            $ref: ../schemas/quota-policy-actions.yaml
    403:
      description: |
        [Forbidden](ref:403). You don't have access to quota policies in this security configuration.
      $ref: ../errors/403.yaml
    404:
      description: |
        [Not found](ref:404). Security configuration version or security policy wasn't found.
      $ref: ../errors/404.yaml
