type: object
description: |
  A result of an import operation, including endpoint details and a list of potential issues.
additionalProperties: false
properties:
  apiEndpointDetails:
    description: |
      The imported endpoint.
    $ref: api-endpoint-with-resource-dto-recurse-response.yaml
  problems:
    type: array
    description: |
      Problems that occurred during the import, such as schema errors or unsupported property warnings.
    items:
      $ref: http-problem-details.yaml
