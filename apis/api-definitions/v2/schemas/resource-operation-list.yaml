type: object
additionalProperties: false
required:
- operations
properties:
  operations:
    type: array
    description: |
      Resource operations.
    readOnly: true
    items:
      $ref: resource-operation.yaml
