description: |
  An endpoint and its associated resources.
allOf:
  - $ref: api-endpoint-with-resource-dto-recurse.yaml
  - type: object
    required:
    - type
    - instance
  - type: object
    properties:
      apiResources:
        type: array
        description: |
          Resources assigned to the endpoint.
        nullable: true
        items:
          $ref: api-resource-meth-params-dto-request.yaml
