type: object
description: |
  Collects data for operation verification.
additionalProperties: false
required:
- operations
properties:
  operations:
    type: array
    description: |
      Operations to verify.
    minItems: 1
    items:
      $ref: verify-operation.yaml
