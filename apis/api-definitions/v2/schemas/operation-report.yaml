type: object
description: |
  Collects data for the operation report.
additionalProperties: false
required:
- apiEndPoints
- operations
- resources
properties:
  apiEndPoints:
    type: array
    description: |
      API endpoints and their data.
    items:
      $ref: api-endpoint-summary.yaml
  operations:
    type: array
    description: |
      Operations and their data.
    items:
      $ref: operation-summary.yaml
  resources:
    type: array
    description: |
      Resources and their data.
    items:
      $ref: resource-summary.yaml
  validation:
    $ref: validation.yaml
