description: |
  An HTTP method to which a resource may respond.
allOf:
  - $ref: api-method-parameters-dto.yaml
  - type: object
    required:
    - type
    - instance
  - type: object
    properties:
      apiParameters:
        type: array
        description: |
          Parameter objects assigned to the method.
        nullable: true
        items:
          $ref: api-parameter-dto-request.yaml
