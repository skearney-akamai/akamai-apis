description: |
  A resource assigned to an endpoint.
allOf:
  - $ref: api-resource-meth-params-dto.yaml
  - type: object
    required:
    - type
    - instance
  - type: object
    properties:
      apiResourceMethods:
        type: array
        description: |
          HTTP methods the resource may respond to.
        items:
          $ref: api-method-parameters-dto-response.yaml
