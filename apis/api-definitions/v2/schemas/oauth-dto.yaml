type: object
description: |
  OAuth scopes configured for an endpoint. You can configure OAuth scopes if the API Gateway product is in your contract and you're taking part in the API Gateway beta program.
additionalProperties: false
required:
- enabled
properties:
  enabled:
    type: boolean
    description: |
      Whether you enabled the OAuth scopes assignment for the endpoint.
  resources:
    $ref: resources-oauth.yaml
