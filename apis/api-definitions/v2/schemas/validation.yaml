type: object
description: |
  Contains warnings, errors, or notices determined by validating the match target.
additionalProperties: false
required:
- notices
- errors
- warnings
properties:
  errors:
    type: array
    description: |
      Errors.
    readOnly: true
    items:
      $ref: validation-dto.yaml
  notices:
    type: array
    description: |
      Notices.
    readOnly: true
    items:
      $ref: validation-dto.yaml
  warnings:
    type: array
    description: |
      Warnings.
    readOnly: true
    items:
      $ref: validation-dto.yaml
