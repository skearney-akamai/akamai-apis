type: object
description: |
  Specifies personally identifiable information (PII) found or defined in an API endpoint version.
additionalProperties: false
properties:
  piis:
    type: array
    description: |
      PII objects describing the kind of PII and where it appears in API payloads.
    items:
      $ref: pii-dto.yaml
