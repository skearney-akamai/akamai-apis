description: |
  An HTTP method to which a resource may respond.
allOf:
  - type: object
    required:
    - type
    - instance
  - $ref: api-method-parameters-dto.yaml
  - type: object
    properties:
      apiParameters:
        type: array
        description: |
          Parameter objects assigned to the method.
        nullable: true
        items:
          $ref: api-parameter-dto-response.yaml
