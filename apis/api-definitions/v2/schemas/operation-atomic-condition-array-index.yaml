type: object
description: |
  The parameter and its index within the array.
additionalProperties: false
required:
- apiParameterId
- index
properties:
  apiParameterId:
    type: integer
    description: |
      Identifies the parameter.
    format: int64
  index:
    type: integer
    description: |
      The location of the parameter's data within the array.
