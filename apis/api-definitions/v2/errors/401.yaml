description: |
  [Unauthorized](ref:401). You aren't authorized to perform the action.
content:
  application/problem+json:
    example:
      $ref: ../examples/401.json
    schema:
      $ref: ../schemas/http-problem-details.yaml
