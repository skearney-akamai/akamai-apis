openapi: 3.0.0
info:
  title: Multistep groups
  version: v2
servers:
- url: https://{hostname}/api-definitions/v2
x-akamai:
  auth-type: EDGE_GRID
paths:
  /endpoints/{endpointId}/versions/{versionNumber}/multistep-groups:
    $ref: paths/multistep-groups.yaml
  /endpoints/{endpointId}/versions/{versionNumber}/multistep-groups/{multistepGroupId}:
    $ref: paths/multistep-group.yaml
