name: queryType
in: query
description: |
  Choose `ACTIVE_IN_PRODUCTION`, `ACTIVE_IN_STAGING`, or `ACTIVE_WITHIN_DATE_RANGE`.
required: false
schema:
  type: string
  example: ACTIVE_WITHIN_DATE_RANGE
  enum:
  - ACTIVE_IN_PRODUCTION
  - ACTIVE_IN_STAGING
  - ACTIVE_WITHIN_DATE_RANGE
