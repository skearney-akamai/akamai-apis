name: sortBy
in: query
description: |
  The field to sort endpoint versions by, `versionNumber` by default.
schema:
  type: string
  example: updateDate
  default: versionNumber
  enum:
  - versionNumber
  - description
  - updateDate
  - updatedBy
  - basedOn
  - stagingStatus
  - productionStatus
