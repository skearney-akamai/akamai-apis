name: excludeResponseContent
in: query
description: |
  Excludes any response body specifications from what's imported, `false` by default.
required: false
schema:
  type: boolean
  example: true
  default: false
