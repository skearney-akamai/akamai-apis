name: sortOrder
in: query
description: |
  The sort order, either `desc` for descending or the default `asc` for ascending.
schema:
  type: string
  default: asc
  example: desc
  enum:
  - asc
  - desc
