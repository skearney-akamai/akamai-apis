name: sortBy
in: query
description: |
  The field to sort endpoints by, either the `updateDate` or the API `name` that corresponds to the `apiEndPointName` in data. The default is `name`.
schema:
  type: string
  example: name
  default: name
  enum:
  - name
  - updateDate
