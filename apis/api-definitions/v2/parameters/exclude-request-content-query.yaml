name: excludeRequestContent
in: query
description: |
  Excludes any request body specifications from what's imported, `false` by default.
required: false
schema:
  type: boolean
  example: true
  default: false
